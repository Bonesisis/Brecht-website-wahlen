# Server-Konfiguration
PORT=3000

# CORS - Frontend-URL(s), kommagetrennt für mehrere
# Beispiele:
#   CORS_ORIGIN=*                              # Alle erlauben (Entwicklung)
#   CORS_ORIGIN=https://username.github.io    # Nur GitHub Pages
#   CORS_ORIGIN=http://localhost:5500,https://username.github.io
CORS_ORIGIN=*

# Frontend als Static-Files ausliefern (true/false)
# Wenn true, wird der Parent-Ordner (../) als statische Dateien ausgeliefert
SERVE_FRONTEND=false

# JWT-Konfiguration
JWT_SECRET=brechtwahl-geheim-aendern-in-produktion
JWT_EXPIRES_IN=7d

# Admin-Code für geschützte Endpoints
# Dieser Code muss im Header X-Admin-Code mitgeschickt werden
ADMIN_CODE=brecht-admin-2026
